# Public domain & origin used by Caddy, CORS, and the frontend API base.
# Local: leave as-is (admin will be at http://admin.localhost).
PUBLIC_DOMAIN=localhost
PUBLIC_ORIGIN=http://localhost

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=pureblog
POSTGRES_DB=pureblog

# Backend secrets / first-run admin seed
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=muxi
ADMIN_PASSWORD=change-me
